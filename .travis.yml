matrix:
    include:
        - os: linux
          sudo: required
          dist: trusty
          language: generic
        - os: osx
          language: generic
          osx_image: xcode7.3

cache:
  directories:
  - dependencies/linux
  - dependencies/osx

before_install: date +%s > start_time
  
install:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then ./reproducible_build_osx.sh -t -d; fi


script: true
  #- if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then ./reproducible_build.sh -t; fi
  #- if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then ./reproducible_build_osx.sh -t -l; fi
